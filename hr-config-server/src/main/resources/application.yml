server:
  port: 8888

spring:
  application:
    name: hr-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GabrielRodrigues07/ms-course-configs.git
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_PASSWORD}