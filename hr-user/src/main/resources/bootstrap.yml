spring:
  application:
    name: hr-user
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info
#  config:
#    import: file:./src/main/resources/bootstrap.yml