spring:
  application:
    name: hr-api-gateway
  cloud:
    config:
      uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info
#  config:
#    import: file:./src/main/resources/bootstrap.yml